# Backend (server/index.js) — clave secreta de Stripe, nunca la expongas en el frontend
STRIPE_SECRET_KEY=sk_test_...

# Frontend (Vite) — clave publicable de Stripe, es segura para exponer en el navegador
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Puerto del servidor Express (opcional)
PORT=8787

# Backend (server/index.js) — clave secreta de MaxelPay para crear sesiones
# de pago en cripto, nunca la expongas en el frontend.
MAXELPAY_API_KEY=pk_live_...

# Opcional: solo si MaxelPay te da una URL de API distinta a la de por defecto.
MAXELPAY_API_URL=https://api.maxelpay.com/api/api/v1/payments/sessions
